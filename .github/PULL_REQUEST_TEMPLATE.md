## What changed and why

<!-- Summarize the change and the motivation behind it. -->

## Related design doc

<!-- If this is a design-shaping change, link the relevant doc under docs/. Otherwise, delete this section. -->

## Checklist

- [ ] `go build ./...` passes
- [ ] `go vet ./...` passes
- [ ] `go test -race ./...` passes
- [ ] `gofmt -l .` reports no files
